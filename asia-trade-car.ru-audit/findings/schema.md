# Schema — 82/100

Исправлено: `Product + Car`, truthful Offer, brand/model/sku/condition/images, расширенная Organization со стабильным `@id`.

Осталось:

- Medium: нет `BreadcrumbList` для видимых breadcrumbs.
- Medium/Low: нет `WebSite`; `Service` можно добавить после полного совпадения с visible content.
- Low: team/reviews не представлены отдельными проверяемыми сущностями.

